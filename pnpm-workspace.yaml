packages:
  - "."

# Nest 12.0.0-alpha.* still declares ^11.0.0 peers on its own siblings.
peerDependencyRules:
  allowedVersions:
    "@nestjs/common": "12"
    "@nestjs/core": "12"
    "@nestjs/platform-express": "12"
    "@nestjs/websockets": "12"
    "@nestjs/microservices": "12"

# @nestjs/core's postinstall is only a donation banner; never run it.
allowBuilds:
  "@nestjs/core": false
